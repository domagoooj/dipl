from ui import App

if __name__ == "__main__":
    App().mainloop()
     # calls the App constructor, creates tkinter window
     # the program processes mouse, keyboard, etc. It return when window closes -> program exits


